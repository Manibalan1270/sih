; Webots reads this before launching the controller. On Windows the plain
; "python" command is often the Store alias, so name the launcher explicitly.
; If your Python is elsewhere, set Preferences > General > Python command instead.
[python]
COMMAND = py
