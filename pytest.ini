[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    tc: an SRS Section 6.2 verification test case
    slow: takes more than a few seconds
addopts = -ra --strict-markers
